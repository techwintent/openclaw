# Long-Term Memory

> Curated, durable memory. Append-only by `memory-extractor`; periodically distilled by `dreaming` worker.
>
> ⚠️ **Loaded only in main sessions** (direct chat with the tenant). Never load in shared / group / customer-facing contexts.

---

## 🎯 Current Position

(High-level: what is this tenant doing right now? What's the active priority?)

---

## 💡 Core Insights

(Distilled lessons that should outlive any single conversation.)

- 

---

## 🔑 Key Decisions

(Pointers to `memory/decisions/DECISION-*.md` for major decisions.)

- 

---

## 📌 Stable Facts

(Things that don't change often: business model, key customer profiles, recurring constraints.)

- 

---

## Recent Entries

(Appended by `memory-extractor`. Each entry has YAML frontmatter with timestamp, source message id, and tags.)

<!-- entries appended below this line -->
